# Ruff 配置 — Python linter & formatter
# 仅用于 TController/ 下的 Python 代码

[lint]
# 忽略不适用于本项目的规则
ignore = [
    "N999",    # 模块名大写开头（Communication/DataProcessor 是项目命名约定）
    "S110",    # try/except/pass（GUI 串口/IO 兜底场景）
    "BLE001",  # 裸 except Exception（同上，防御性捕获）
]
